
To build the jni library use the following steps:

1) Download Android NDK from https://developer.android.com/ndk/downloads/
2) Extract the downloaded zip archive
3) Run the following command to build
<extracted ndk path>/ndk-build -B V=1 

This will build and copy the libs into the appropriate folder.
